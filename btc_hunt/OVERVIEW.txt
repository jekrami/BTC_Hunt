╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║                    BTC HUNT v2.0                             ║
║          Unified Bitcoin Address Hunter                      ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│ WHAT IT DOES                                                 │
└──────────────────────────────────────────────────────────────┘

  1. Generates random BIP39 mnemonic phrases (12 words)
  2. Derives Bitcoin addresses (60 per mnemonic, multiple paths)
  3. Checks addresses against your SQLite database
  4. Creates detailed report when match is found

┌──────────────────────────────────────────────────────────────┐
│ QUICK START                                                  │
└──────────────────────────────────────────────────────────────┘

  Build:
    Windows:  .\build.bat
    Linux:    ./build.sh
    Manual:   cargo build --release

  Run:
    Windows:  .\target\release\btc_hunt.exe -d btc_addresses.db
    Linux:    ./target/release/btc_hunt -d btc_addresses.db

  Help:
    Windows:  .\target\release\btc_hunt.exe --help
    Linux:    ./target/release/btc_hunt --help

┌──────────────────────────────────────────────────────────────┐
│ COMMON COMMANDS                                              │
└──────────────────────────────────────────────────────────────┘

  Test run (10 batches of 100):
    btc_hunt -d btc_addresses.db -b 100 -n 10

  Replicate old script (50K per batch):
    btc_hunt -d btc_addresses.db -b 50000

  Maximum performance (10K per batch, auto threads):
    btc_hunt -d btc_addresses.db -b 10000 -t 0

  Low memory (500 per batch, 5 addresses per path):
    btc_hunt -d btc_addresses.db -b 500 -a 5

┌──────────────────────────────────────────────────────────────┐
│ PERFORMANCE vs OLD SETUP                                     │
└──────────────────────────────────────────────────────────────┘

  Old (3 programs + bash):    5 minutes per batch (50K seeds)
  New (unified):              1 minute per batch (50K seeds)
  
  ⚡ 5× FASTER OVERALL
  ⚡ 70× FASTER database queries
  ⚡ Parallel processing on all CPU cores
  ⚡ Zero file I/O between steps

┌──────────────────────────────────────────────────────────────┐
│ DOCUMENTATION                                                │
└──────────────────────────────────────────────────────────────┘

  📖 PROJECT_SUMMARY.md     - High-level overview (START HERE!)
  📖 QUICK_START.md         - Get running in 5 minutes
  📖 README.md              - Complete documentation
  📖 MIGRATION_GUIDE.md     - Transition from old setup
  📖 FEATURES.md            - Technical deep-dive
  📖 OVERVIEW.txt           - This file

┌──────────────────────────────────────────────────────────────┐
│ FILE STRUCTURE                                               │
└──────────────────────────────────────────────────────────────┘

  btc_hunt/
  ├── src/
  │   └── main.rs                 ← Complete application (680 lines)
  ├── Cargo.toml                  ← Dependencies
  ├── bip39-english.txt           ← BIP39 wordlist
  ├── build.sh / build.bat        ← Build scripts
  ├── .gitignore                  ← Git ignore rules
  └── *.md                        ← Documentation

  After building:
  └── target/
      └── release/
          └── btc_hunt(.exe)      ← Your optimized binary!

┌──────────────────────────────────────────────────────────────┐
│ KEY FEATURES                                                 │
└──────────────────────────────────────────────────────────────┘

  ✓ Parallel mnemonic generation
  ✓ Parallel address derivation
  ✓ Batched database queries
  ✓ Real-time statistics
  ✓ In-memory pipeline (no file I/O)
  ✓ Immediate stop on match
  ✓ Detailed report generation
  ✓ Cross-platform (Windows/Linux/Mac)
  ✓ Single binary (easy deployment)
  ✓ Configurable everything

┌──────────────────────────────────────────────────────────────┐
│ COMMAND-LINE OPTIONS                                         │
└──────────────────────────────────────────────────────────────┘

  -d, --database <PATH>              Database file path
  -b, --batch-size <N>               Mnemonics per batch [1000]
  -n, --max-batches <N>              Max batches (0=unlimited) [0]
  -a, --addresses-per-path <N>       Addresses per path [10]
  -o, --output-dir <PATH>            Report directory [results]
  -t, --threads <N>                  Worker threads (0=auto) [0]
  -s, --stats-interval <SEC>         Stats interval [5]
  --db-batch-size <N>                DB batch size [1000]
  -h, --help                         Show help
  -V, --version                      Show version

┌──────────────────────────────────────────────────────────────┐
│ DERIVATION PATHS                                             │
└──────────────────────────────────────────────────────────────┘

  m/0'/0'/{i}'              Legacy Electrum (P2PKH)
  m/44'/0'/0'/0/{i}'        BIP44 (P2PKH)
  m/49'/0'/0'/0/{i}'        BIP49 (P2SH-wrapped SegWit)
  m/84'/0'/0'/0/{i}'        BIP84 (Native SegWit)
  m/0/{i}'                  Custom (P2SH-wrapped SegWit)
  m/0/{i}'                  Custom (Native SegWit)

  With -a 10: 6 paths × 10 addresses = 60 addresses per mnemonic

┌──────────────────────────────────────────────────────────────┐
│ DATABASE REQUIREMENTS                                        │
└──────────────────────────────────────────────────────────────┘

  Table:  addresses
  Column: address (TEXT)
  Index:  CREATE INDEX idx_address ON addresses(address);

  ⚠️ INDEX IS CRITICAL FOR PERFORMANCE!

┌──────────────────────────────────────────────────────────────┐
│ WHEN MATCH IS FOUND                                          │
└──────────────────────────────────────────────────────────────┘

  1. Program stops immediately
  2. Displays: "🎉 MATCH FOUND! 🎉"
  3. Saves report to: results/MATCH_FOUND_<timestamp>.txt
  4. Report includes:
     - Mnemonic phrase
     - BIP39 seed (hex)
     - Found addresses
     - All derived addresses with private keys
     - Derivation paths
  5. Exits with code 0

┌──────────────────────────────────────────────────────────────┐
│ STATISTICS OUTPUT                                            │
└──────────────────────────────────────────────────────────────┘

  Updates every 5 seconds (configurable with -s):

  ╔════════════════════════════════════════════════════════════╗
  ║                    BTC HUNT STATISTICS                     ║
  ╠════════════════════════════════════════════════════════════╣
  ║ Runtime:         120 seconds
  ║ Batches:         120
  ║ Mnemonics:       120000 (1000/s)
  ║ Addresses:       7200000 (60000/s)
  ║ Checked:         7200000
  ╚════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                              │
└──────────────────────────────────────────────────────────────┘

  Build errors:
    - Install Rust: https://rustup.rs/
    - Update Rust: rustup update
    - Clean build: cargo clean && cargo build --release

  Slow performance:
    - Check database has index
    - Try -t 0 (auto threads)
    - Use SSD for database
    - Increase batch size: -b 10000

  Out of memory:
    - Reduce batch size: -b 500
    - Reduce addresses: -a 5

  Database not found:
    - Use absolute path: -d /full/path/to/db.db
    - Check file exists: ls btc_addresses.db

┌──────────────────────────────────────────────────────────────┐
│ TIPS & TRICKS                                                │
└──────────────────────────────────────────────────────────────┘

  • Use -t 0 for auto thread detection (recommended)
  • Larger batches = better throughput, more memory
  • Create database index for 70× faster queries
  • Use SSD for database storage
  • Monitor with -s 1 for per-second updates
  • Test with -n 10 before long runs
  • Keep database on same drive as binary
  • Close unnecessary apps for more CPU/RAM

┌──────────────────────────────────────────────────────────────┐
│ SECURITY NOTES                                               │
└──────────────────────────────────────────────────────────────┘

  ✓ Uses OS random generator (cryptographically secure)
  ✓ No network communication
  ✓ No external services
  ✓ Open source code

  ⚠️ Reports contain private keys in plain text
  ⚠️ Keys visible in memory during processing
  ⚠️ No built-in encryption

  Recommendations:
  • Run on isolated/offline machine
  • Encrypt reports immediately
  • Delete reports after securing
  • Use full-disk encryption
  • Don't run as admin/root

┌──────────────────────────────────────────────────────────────┐
│ COMPARISON: OLD vs NEW                                       │
└──────────────────────────────────────────────────────────────┘

  OLD SETUP:
  ┌──────────────────┐
  │ bip39_generator  │  Generate seeds
  └────────┬─────────┘
           │ seeds.txt (FILE I/O)
           ▼
  ┌──────────────────┐
  │ btc-key-deriver  │  Derive addresses
  └────────┬─────────┘
           │ addressonly.txt (FILE I/O)
           ▼
  ┌──────────────────┐
  │   db_search      │  Check database
  └────────┬─────────┘
           │
  ┌────────▼─────────┐
  │ run_until_found  │  Bash orchestration
  └──────────────────┘

  Problems: Slow I/O, sequential, one DB query per address

  NEW SETUP:
  ┌──────────────────────────────────────┐
  │           btc_hunt                   │
  │  ┌────────────┐                      │
  │  │  Generate  │ (parallel, in-memory)│
  │  └─────┬──────┘                      │
  │        │                              │
  │  ┌─────▼──────┐                      │
  │  │   Derive   │ (parallel, in-memory)│
  │  └─────┬──────┘                      │
  │        │                              │
  │  ┌─────▼──────┐                      │
  │  │   Check    │ (batched queries)    │
  │  └─────┬──────┘                      │
  │        │                              │
  │  ┌─────▼──────┐                      │
  │  │   Report   │ (on match)           │
  │  └────────────┘                      │
  └──────────────────────────────────────┘

  Benefits: Fast, parallel, no I/O, simple

┌──────────────────────────────────────────────────────────────┐
│ NEXT STEPS                                                   │
└──────────────────────────────────────────────────────────────┘

  1. Read PROJECT_SUMMARY.md (comprehensive overview)
  2. Build: ./build.sh or .\build.bat
  3. Test: btc_hunt -d btc_addresses.db -b 100 -n 10
  4. Run:  btc_hunt -d btc_addresses.db -b 50000
  5. Monitor statistics and tune performance
  6. Enjoy your 5× faster Bitcoin hunting! 🚀

┌──────────────────────────────────────────────────────────────┐
│ CREDITS                                                      │
└──────────────────────────────────────────────────────────────┘

  Original tools:       Jafar Ekrami
  Unified version:      BTC Hunt v2.0
  Date:                 January 2026
  Purpose:              Retirement programming project
  
  From 3 separate tools to 1 unified powerhouse!

╔══════════════════════════════════════════════════════════════╗
║                  HAPPY HUNTING! 🎯🚀                         ║
╚══════════════════════════════════════════════════════════════╝



